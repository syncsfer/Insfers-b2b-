/**
 * @deprecated Moved to `@/lib/email`. Re-exported here so existing imports keep
 * working; point new code at `@/lib/email` directly.
 */
export {
  renderReceiptEmail,
  fillTemplate,
  type RenderedEmail,
} from '@/lib/email';
